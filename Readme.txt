Input:
	The input file should have the graph stored as an adjacency list.
	Vertices are separated by spaces and entries are separated by newlines.
	
Argument:
	Input an integer after the executable to indicate k for the resulting k-cliques. 
	The default is k = 4 if no specific k is given.

Return:
	Each returning string represents one k-cliques. 